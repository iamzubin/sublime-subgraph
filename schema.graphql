enum Status {
  VERIFIED
  UNVERIFIED
}



type Pool @entity {
  id: ID!
  borrower: User!
  details: BigInt
}

type User @entity {
  id: ID!
  borrowingPools: [Pool!] @derivedFrom(field: "borrower")
}
